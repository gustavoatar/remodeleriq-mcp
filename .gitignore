# Dependencies
node_modules/

# Build output
dist/

# Wrangler local state
.wrangler/

# Internal / runtime DBs
agentdb.rvf
agentdb.rvf.lock
ruvector.db
graphify-out/

# Local plugin caches (RuVector index DBs — regenerated on demand)
.plugin-state/

# Draft content (not source code)
blog-drafts/
tmp-r2-migration/

# OS
.DS_Store
Thumbs.db

# Local env overrides (secrets go in `wrangler secret put`, not files)
.env
.env.local
.dev.vars
.secrets-restore.json
